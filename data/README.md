# Local data directory

Raw datasets must not be committed to this repository.

Expected local layout:

```text
data/
├── aptos2019/
├── eyepacs/
└── messidor2/
```

See `docs/DATASETS.md` for dataset provenance and reproducibility notes.
